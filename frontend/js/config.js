/**
 * NEAR DAIRY - FRONTEND CONFIGURATION
 * 
 * Instructions:
 * 1. Deploy your backend to Render (e.g. https://neardairy-backend.onrender.com).
 * 2. Paste your Render backend URL into `BACKEND_URL` below.
 * 3. In local development (localhost), it automatically connects to http://localhost:3000.
 */

window.APP_CONFIG = {
  // Set your deployed Render Backend URL here:
  // Example: 'https://neardairy-backend.onrender.com'
  BACKEND_URL: '',
};
